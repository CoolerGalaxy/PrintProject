$(function() {
    function PrintFarmerViewModel(parameters) {
        var self = this;

		// we're set up to write some jquery
		
    }

    OCTOPRINT_VIEWMODELS.push([
        PrintFarmerViewModel,
        ["settingsViewModel"]
    ]);
});